{% extends 'base.html.twig' %}

{% block title %}Parcelle{% endblock %}

{% block body %}
    <h1>Parcelle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ parcelle.numero }}</td>
            </tr>
            <tr>
                <th>Disponibilité</th>
                <td style="background: {{maison.disponibilite ? 'lightgreen' : 'red'}}">{{ parcelle.disponibilite ? 'Oui' : 'Non' }}</td>
            </tr>
            <tr>
                <th>Superficie</th>
                <td>{{ parcelle.superficie }}</td>
            </tr>
        </tbody>
    </table>
    {% if is_granted('ROLE_ADMIN') %}

    <a href="{{ path('app_parcelle_index') }}" class="link-info">revoir la liste</a>

    <a href="{{ path('app_parcelle_edit', {'id': parcelle.id}) }}" class="link-secondary link">editer</a>

    {{ include('parcelle/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
