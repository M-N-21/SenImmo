{% extends 'base.html.twig' %}

{% block title %}Localite{% endblock %}

{% block body %}
    <h1>Localite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Region</th>
                <td>{{ localite.region }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ localite.ville }}</td>
            </tr>
            <tr>
                <th>Quartier</th>
                <td>{{ localite.quartier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_localite_index') }}" class="link-info">revoir la liste</a>

    <a href="{{ path('app_localite_edit', {'id': localite.id}) }}" class="link-secondary">editer</a>

    {{ include('localite/_delete_form.html.twig') }}
{% endblock %}
