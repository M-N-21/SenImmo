{% extends 'base.html.twig' %}

{% block title %}Editer User{% endblock %}

{% block body %}
    <h1>Editer User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_index') }}" class="link-info">revoir la liste</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
